Place these two files in this folder before building:

uniprint.ico  — Small square icon (256×256 recommended, multi-size ICO)
               Used for: system tray, window title bar, NSIS installer

uniprint.png  — Wide/horizontal logo (e.g. 200×40 or similar landscape ratio)
               Used for: the header bar inside the app UI
